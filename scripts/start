#!/usr/bin/env bash
echo "Resetting this directory to mirror what's on github"
echo ""
echo "If this fails, it's probably due to the internet not working. Try again later!"

git fetch && git reset --hard origin/master
